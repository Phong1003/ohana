import Vue from 'vue';
import VueViewer from 'v-viewer';

// export default ({ store }) => {
//     new Paginate({
//         /* your options */
//     }).plugin(store);
// }
Vue.component('VueViewer', VueViewer);
